import {
    COLOR_LEAD_WHITE,
} from '../../constants';

const styles = {

    container: {
        // styling self
        width: '100vw',
        height: '100vh',
        backgroundColor: COLOR_LEAD_WHITE,
    },

};

export default styles;
